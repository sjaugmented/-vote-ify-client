// const url = 

export default class PlaylistModel {
    static all = async () => {
        try {
            
        } catch (error) {
            console.log(error)
        }
    }
    static show = async (playlistId) => {
        try {
            
        } catch (error) {
            console.log(error)
        }
    }
}